version: 2
jobs:
  build:
    docker: 
      - image: node:8.0
    steps:
      - run:
        name: "Checking Version"
        command: |
          node --version
          npm --version
      - run:
        name: "Install npm dependencies"
        command: npm install
      - run:
        name: "Start Server"
        command: npm start 